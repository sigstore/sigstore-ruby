#!/usr/bin/env ruby
# frozen_string_literal: true

command = ARGV.shift

exec({ "BUNDLE_GEMFILE" => File.expand_path("../Gemfile", __dir__) },
     "bundle", "exec", "gem", "sigstore_cosign_#{command.tr("-", "_")}", *ARGV)
